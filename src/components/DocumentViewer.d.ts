/// <reference types="react" />
export declare type TProps = {
    docUrl: string;
    UID: string;
};
declare function DocumentViewer({ docUrl, UID }: TProps): JSX.Element;
export default DocumentViewer;
